Issue #{number} in {org}/{repo}: {title}

Action: {action} (from label ac-{action})

Issue Body:
{body}

{comments_section}

IMPORTANT — work efficiently:
- If the issue mentions specific files or paths, start there. Do NOT explore the whole codebase.
- Read the CLAUDE.md file first if one exists — it has build/test commands and project structure.
- Make targeted, minimal changes. Do not refactor surrounding code.
- Run tests only if CLAUDE.md or package.json/pyproject.toml tells you how.
- Do not add comments, docstrings, or type annotations to code you didn't change.
- Do not install new dependencies unless the issue specifically requires them.

When complete, output a line starting with IMPLEMENTATION_SUMMARY:
followed by a one-sentence description of what you did.
